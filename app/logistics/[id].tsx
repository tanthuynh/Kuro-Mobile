/**
 * app/logistics/[id].tsx
 * Backward compatibility re-export stub pointing to nested tab stack (tabs)/logistics/[id]
 */

export { default } from '../(tabs)/logistics/[id]';
export * from '../(tabs)/logistics/[id]';
