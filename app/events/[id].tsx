/**
 * app/events/[id].tsx
 * Backward compatibility re-export stub pointing to nested tab stack (tabs)/events/[id]
 */

export { default } from '../(tabs)/events/[id]';
export * from '../(tabs)/events/[id]';
