/**
 * app/repair/[id].tsx
 * Backward compatibility re-export stub pointing to nested tab stack (tabs)/repairs/[id]
 */

export { default } from '../(tabs)/repairs/[id]';
export * from '../(tabs)/repairs/[id]';
